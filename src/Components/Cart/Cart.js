import React from 'react';

const Cart = ({jewelry}) => {
    // console.log(jewelry);
    return (
        <div>
            <h5>Name: { jewelry.name }</h5>
        </div>
    );
};

export default Cart;